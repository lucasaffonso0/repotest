apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: floodgate-egress-checkout-service-to-notification-worker
  namespace: ecommerce
  labels:
    managed-by: floodgate
    floodgate-policy-type: allow-egress
    source-workload: checkout-service
    source-namespace: ecommerce
    target-service: notification-worker
    target-port: '8080'
spec:
  podSelector:
    matchLabels:
      app: checkout-service
  policyTypes:
    - Egress
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: messaging
          podSelector:
            matchLabels:
              app: notification-worker
      ports:
        - port: 8080
          protocol: TCP
    - ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
