apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: floodgate-egress-product-catalog-to-inventory-db
  namespace: ecommerce
  labels:
    managed-by: floodgate
    floodgate-policy-type: allow-egress
    source-workload: product-catalog
    source-namespace: ecommerce
    target-service: inventory-db
    target-port: '8080'
spec:
  podSelector:
    matchLabels:
      app: product-catalog
  policyTypes:
    - Egress
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: data
          podSelector:
            matchLabels:
              app: inventory-db
      ports:
        - port: 8080
          protocol: TCP
    - ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
